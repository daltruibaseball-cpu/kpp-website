# GoHighLevel (required for lead submission to CRM)
# Get Location ID from GHL: Settings → Business Info
# Create API key with contacts.write (and contacts.readonly for upsert) for that location
GHL_LOCATION_ID=
GHL_API_KEY=

# Resend (optional – email notification on new lead)
RESEND_API_KEY=
